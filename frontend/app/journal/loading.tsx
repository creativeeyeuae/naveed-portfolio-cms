// Covers /journal/[slug] since there is no standalone /journal index page --
// a segment's loading.tsx wraps its nested dynamic child routes too.
// See components/RouteLoading.tsx for what this renders and why.
export { default } from "@/components/RouteLoading";
