// See components/RouteLoading.tsx for what this renders and why. Covers /work AND
// /work/[slug] (a segment's loading.tsx also wraps its nested dynamic routes).
export { default } from "@/components/RouteLoading";
